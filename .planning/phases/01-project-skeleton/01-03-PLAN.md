---
phase: 01-project-skeleton
plan: 03
type: tdd
wave: 2
depends_on: [01-01]
files_modified: [app/src/test/kotlin/com/example/AppTest.kt, app/src/test/kotlin/com/example/healthTest.kt]
autonomous: true
requirements: [SETUP-02]

must_haves:
  truths:
    - "Tests compile"
    - "Tests pass with bazel test //..."
    - "AppTest has working test for health"
  artifacts:
    - path: "app/src/test/kotlin/com/example/AppTest.kt"
      provides: "Application test setup/teardown"
      min_lines: 20
    - path: "app/src/test/kotlin/com/example/healthTest.kt"
      provides: "Health endpoint test"
      min_lines: 30
  key_links:
    - from: "healthTest.kt"
      to: "App.kt"
      via: "embeddedServer test"
      pattern: "embeddedServer"
---

<objective>
Set up test infrastructure

Purpose: Enable TDD workflow. Tests establish the contract and verify behavior.
Output: Test infrastructure with JUnit5/Kotest that passes
</objective>

<execution_context>
@/Users/fall_out_bug/projects/vibe_coding/sdp-trace-demo-jvm-gsd/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/fall_out_bug/projects/vibe_coding/sdp-trace-demo-jvm-gsd/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD test infrastructure</name>
  <files>app/src/test/kotlin/com/example/AppTest.kt</files>
  <behavior>
    - Test: health endpoint returns 200 with {status: "ok"}
    - Test: health endpoint returns Content-Type: application/json
    - Test: health endpoint includes timestamp field
  </behavior>
  <action>Create AppTest.kt using JUnit5 with Kotest:
- package com.example
- Use testApplication { } block from ktor-test
- client.get("/health") should respond with 200
- response.body<JsonObject>() should have "status" = "ok"
- response.body<JsonObject>() should have "timestamp"

Use Kotestmatchers for assertions.</action>
  <verify>
<automated>bazel test //app:all_tests --test_output=all 2>&1 | tail -20</automated>
  </verify>
  <done>Health test passes, verifies 200 and JSON response</done>
</task>

<task type="auto">
  <name>Task 2: Ensure test BUILD files</name>
  <files>app/BUILD.bazel (update test target)</files>
  <action>Update app/BUILD.bazel with:
- kotlin_test target named "all_tests"
- Test files: AppTest.kt, healthTest.kt  
- deps: kotlin_test, ktor_server_test, kotest_assertions, kotlinx_serialization_json
- Add kotlinx_coroutines for test async

Test target should be separate from main binary TARGETS.</action>
  <verify>
<automated>bazel query //app:all_tests 2>&1 | grep "all_tests"</automated>
  </verify>
  <done>Test TARGET exists in query</done>
</task>

</tasks>

<verification>
- Verify: bazel test //... passes
- Verify: kotest matchers work in test context
</verification>

<success_criteria>
- [ ] SETUP-02: Tests pass with `bazel test //...`
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton/01-03-SUMMARY.md`
</output>