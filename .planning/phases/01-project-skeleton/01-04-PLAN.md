---
phase: 01-project-skeleton
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified: [.github/workflows/ci.yaml]
autonomous: true
requirements: [SETUP-03]

must_haves:
  truths:
    - "CI workflow exists in .github/workflows"
    - "CI triggers on pull request"
    - "CI runs bazel build and test"
  artifacts:
    - path: ".github/workflows/ci.yaml"
      provides: "GitHub Actions CI pipeline"
      min_lines: 35
  key_links:
    - from: "ci.yaml"
      to: "bazel"
      via: "bazel build //..."
      pattern: "bazel build"
---

<objective>
Create CI pipeline with GitHub Actions

Purpose: Automated verification on PR. Ensures build and tests pass in CI.
Output: .github/workflows/ci.yaml that runs on PR
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions workflow directory</name>
  <files>.github/workflows</files>
  <action>Create .github/workflows directory:
- mkdir -p .github/workflows
- This is a prerequisite directory only</action>
  <verify>
<automated>ls -la .github/workflows/</automated>
  </verify>
  <done>Directory exists</done>
</task>

<task type="auto">
  <name>Task 2: Create ci.yaml workflow</name>
  <files>.github/workflows/ci.yaml</files>
  <action>Create .github/workflows/ci.yaml with:
- name: CI
- on: [push, pull_request]
- runs-on: ubuntu-latest
- steps:
  - uses: actions/checkout@v4
  - name: Setup Bazel
    uses: bazelbuild/setup-bazelisk@v1
  - name: Build
    run: bazel build //...
  - name: Test
    run: bazel test //...

Simple, direct workflow. No caching in v1 - add caching later if needed.</action>
  <verify>
<automated>ls .github/workflows/ci.yaml</automated>
  </verify>
  <done>CI workflow file created</done>
</task>

</tasks>

<verification>
- Verify: File exists with valid YAML structure
- Verify: Contains bazel build and test steps
- Verify: Triggers on push and pull_request
</verification>

<success_criteria>
- [ ] SETUP-03: CI pipeline triggers on pull request
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton/01-04-SUMMARY.md`
</output>