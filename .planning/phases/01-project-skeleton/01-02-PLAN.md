---
phase: 01-project-skeleton
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [app/src/main/kotlin/com/example/App.kt, app/src/main/kotlin/com/example/health.kt]
autonomous: true
requirements: [SETUP-04]

must_haves:
  truths:
    - "Application starts successfully"
    - "Health endpoint returns 200 OK"
    - "Application binary runs without errors"
  artifacts:
    - path: "app/src/main/kotlin/com/example/App.kt"
      provides: "Main application entry point"
      min_lines: 25
    - path: "app/src/main/kotlin/com/example/health.kt"
      provides: "Health endpoint routing"
      min_lines: 20
  key_links:
    - from: "App.kt"
      to: "health.kt"
      via: "routing { health() }"
      pattern: "routing.*health"
---

<objective>
Create Ktor application with health endpoint

Purpose: Basic skeleton application that starts and exposes health check. All other phases build on this running service.
Output: Application that starts and returns 200 OK from /health
</objective>

<execution_context>
@/Users/fall_out_bug/projects/vibe_coding/sdp-trace-demo-jvm-gsd/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/fall_out_bug/projects/vibe_coding/sdp-trace-demo-jvm-gsd/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create App.kt main entry point</name>
  <files>app/src/main/kotlin/com/example/App.kt</files>
  <action>Create App.kt with:
- package com.example
- Embedded server with Netty (ktor-server-netty-embedded)
- port from env var PORT (default 8080)
- Install StatusPages and ContentNegotiation features
- Configure Kotlinx Serialization JSON
- Start embeddedServer in main()

Use the embedded server pattern from Ktor documentation.</action>
  <verify>
<automated>ls app/src/main/kotlin/com/example/App.kt</automated>
  </verify>
  <done>App.kt exists and contains embeddedServer</done>
</task>

<task type="auto">
  <name>Task 2: Create health.kt endpoint</name>
  <files>app/src/main/kotlin/com/example/health.kt</files>
  <action>Create health.kt with:
- package com.example
- Routing function for health() route
- GET /health returns { "status": "ok", "timestamp": <current timestamp> }
- Use Instant.now() for timestamp
- Application.yml config as default status
- Add ContentType.Application.Json

Simple, stateless health check - no database dependency.</action>
  <verify>
<automated>ls app/src/main/kotlin/com/example/health.kt</automated>
  </verify>
  <done>Health endpoint file exists</done>
</task>

<task type="auto">
  <name>Task 3: Wire health route into App</name>
  <files>BUILD.bazel (update)</files>
  <action>Update app/src/main/kotlin/com/example/App.kt to register health route:
- Import com.example.health
- Add routing { health() } after StatusPages
- Keep minimal - no other routing for now

This creates the complete route chain.</action>
  <verify>
<automated>grep -n "health()" app/src/main/kotlin/com/example/App.kt</automated>
  </verify>
  <done>App.kt imports and calls health()</done>
</task>

</tasks>

<verification>
Local verification:
- Run: bazel run //app:app &
- Verify: curl http://localhost:8080/health returns 200

Note: Requires running server in background and curl. Use checkpoint if needed.
</verification>

<success_criteria>
- [ ] SETUP-04: Application starts and exposes health endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton/01-02-SUMMARY.md`
</output>