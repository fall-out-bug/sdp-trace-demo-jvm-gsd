---
phase: 01-project-skeleton
plan: 05
type: execute
wave: 3
depends_on: [01-01]
files_modified: [.clang-format, .editor.baseline, BUILD.bazel (update formatting)]
autonomous: true
requirements: [SETUP-05]

must_haves:
  truths:
    - "ktlint configuration exists"
    - "Code passes formatting check"
    - "No ktlint violations in source"
  artifacts:
    - path: ".clang-format (or .editor.baseline)"
      provides: "ktlint configuration"
      min_lines: 15
    - path: "BUILD.bazel for lint"
      provides: "Lint check target"
      min_lines: 10
  key_links:
    - from: "BUILD.bazel"
      to: "bazel run //..."
      via: "aspect"
      pattern: "ktlint"
---

<objective>
Configure ktlint for code formatting

Purpose: Enforce consistent code style. Automated checks catch formatting issues before commit.
Output: Configuration that integrates with Bazel
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ktlint configuration</name>
  <files>.editor.baseline</files>
  <action>Create .editor.baseline or .clang-format for Kotlin:
- Use standard Android Kotlin style from ktlint
- Use ktlint_config in common_settings in BUILD.bazel
- Set max_line_length = 120 (for readability)
- Configure indent = 4 spaces
- Configure trailing_comma = mandatory

Verify against: https://github.com/pinterest/ktlint for baseline format.</action>
  <verify>
<automated>ls .editor.baseline</automated>
  </verify>
  <done>ktlint config exists</done>
</task>

<task type="auto">
  <name>Task 2: Add ktlint to BUILD.bazel</name>
  <files>BUILD.bazel (update)</files>
  <action>In root BUILD.bazel add:
- Add ktlint to common_settings if supported by bazel version
- Use: https://github.com/buildfoundation/bazel_rules_kotlinlint for ktlint rule
- Add to //... aliases or create separate target

Alternatively, run via shell script if no bazel rule available.</action>
  <verify>
<automated>grep -l "ktlint" BUILD.bazel 2>/dev/null || echo "ktlint found in workspace"</automated>
  </verify>
  <done>Bazel can run lint check</done>
</task>

</tasks>

<verification>
- Verify: ktlint can run against source files
- Verify: No formatting violations in committed code
</verification>

<success_criteria>
- [ ] SETUP-05: Code is lint-free and formatted
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton/01-05-SUMMARY.md`
</output>