---
phase: 01-project-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [WORKSPACE, BUILD.bazel, app/BUILD.bazel]
autonomous: true
requirements: [SETUP-01]

must_haves:
  truths:
    - "Bazel WORKSPACE is initialized with Kotlin toolchain"
    - "Project compiles with bazel build //..."
    - "Application binary target exists"
  artifacts:
    - path: "WORKSPACE"
      provides: "Bazel workspace with Kotlin and Ktor dependencies"
      min_lines: 30
    - path: "BUILD.bazel"
      provides: "Root build configuration"
      min_lines: 15
    - path: "app/BUILD.bazel"
      provides: "Application binary target"
      min_lines: 20
  key_links:
    - from: "WORKSPACE"
      to: "BUILD.bazel"
      via: "workspace status"
      pattern: "bazel build"
---

<objective>
Create Bazel workspace with Kotlin toolchain and application binary target

Purpose: Establish the build foundation. All subsequent phases depend on this working build.
Output: WORKSPACE, root BUILD.bazel, app/BUILD.bazel with kotlin_binary
</objective>

<execution_context>
@/Users/fall_out_bug/projects/vibe_coding/sdp-trace-demo-jvm-gsd/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/fall_out_bug/projects/vibe_coding/sdp-trace-demo-jvm-gsd/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Bazel WORKSPACE with Kotlin/Ktor dependencies</name>
  <files>WORKSPACE</files>
  <action>Create WORKSPACE file with:
- kotlin_repository() for Kotlin 1.9.x toolchain
- kotlin_stdlib_libs dependencies
- ktor_server for HTTP (select appropriate version compatible with Kotlin 1.9.x)
- kotlinx_serialization for JSON
- sqlcipher for SQLite (embedded)
- Configure java_runtime to JDK 17

Use explicit version numbers. Add necessary http_archive or artifact for embedded-server flavor of Ktor.</action>
  <verify>
<automated>bazel build //... 2>&1 | grep -E "ERROR|SUCCESS|Building"</automated>
  </verify>
  <done>Bazel can resolve all dependencies without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create root BUILD.bazel configuration</name>
  <files>BUILD.bazel</files>
  <action>Create root BUILD.bazel with:
- common_settings for kotlin_options
- default_package_layering for kotlin_options
- Add kotlin_stdlib dependency for common use

DO NOT include application targets here - keep root clean.</action>
  <verify>
<automated>bazel query //... --output=label 2>&1 | head -20</automated>
  </verify>
  <done>Root BUILD.bazel is valid and query returns labels</done>
</task>

<task type="auto">
  <name>Task 3: Create app/BUILD.bazel with kotlin_binary</name>
  <files>app/BUILD.bazel</files>
  <action>Create app/BUILD.bazel with:
- kotlin_binary target named "app"
- main = com.example.AppKt (placeholder for now)
- runtime deps: kotlin_stdlib, ktor_server_core, ktor_server_netty_embedded, kotlinx_serialization_json
- Add Stony Kotlin plugin for embedded server

This is a placeholder binary that will be wired in Plan 02.</action>
  <verify>
<automated>bazel build //app:app --sandbox_debug 2>&1 | tail -10</automated>
  </verify>
  <done>//app:app target compiles successfully</done>
</task>

</tasks>

<verification>
Phase-level verification after all plans:
- Verify: bazel build //... succeeds
- Verify: bazel query //... returns labels
</verification>

<success_criteria>
- [ ] SETUP-01: `bazel build //...` compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton/01-01-SUMMARY.md`
</output>